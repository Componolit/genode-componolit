build "core init gravel/server/parpen"

create_boot_directory

install_config {
    <config>
        <parent-provides>
            <service name="LOG"/>
            <service name="PD"/>
            <service name="CPU"/>
            <service name="ROM"/>
        </parent-provides>
        <default-route>
            <any-service> <parent/> </any-service>
        </default-route>
        <default caps="60"/>
        <start name="parpen">
            <resource name="RAM" quantum="2M"/>
        </start>
    </config>
}

build_boot_image "core ld.lib.so spark.lib.so basalt.lib.so gneiss.lib.so init parpen"

append qemu_args "-nographic "
append xen_args  { sdl="0" }

run_genode_until ".*exited with exit value 0.*\n" 20
